import React from "react";
import "../styles/launcher.module.css"; // Import CSS module

const Launcher = () => {
    return (
        <div className="launcher">
            <button className="launch-button">Launch Event</button>
        </div>
    );
};

export default Launcher;
